import { Router } from "express";

const router = Router();

// get all passes
router.get('/', 
)

// get passes by type
router.get('/types/:type', 
)

// get pass by id
router.get('/:id',)

router.put('/:id',)

router.delete('/:id',)

export default router;